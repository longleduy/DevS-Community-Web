import React, { memo } from 'react';
//Todo: Component
import SignUpProvider from '../../components/SignPage/SignUpForm';

const SignUpContainer = memo(props => {
    return <SignUpProvider />
})
export default SignUpContainer;